services:
  - type: web
    name: makereoke
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: node server.js
    envVars:
      - key: NODE_VERSION
        value: "22.11.0"
    # Render asigna la variable PORT automáticamente
    # El servidor la lee desde process.env.PORT
